name: "buildabear"
category: misc
description: |
  As part of their ongoing cloud migration, Mugjournal has started testing building some barebones code repos on AWS. Every developer has credentials to access the build service, so hopefully everything is locked down beyond that.
value: 350
type: standard

image: null
host: null
flags:
  - flag{99aab3ba909324f3bce9903369c35e69}

# Topics are used to help tell what techniques/information a challenge involves
# They are generally only visible to admins
topics:
  - aws

# Provide paths to files from the same directory that this file is in
files:
  - credentials.json

# Requirements are used to make a challenge require another challenge to be
# solved before being available.
requirements:
  - cloud proxy

# Specifies what version of the challenge specification was used.
# Subject to change until ctfcli v1.0.0
version: "0.1"
